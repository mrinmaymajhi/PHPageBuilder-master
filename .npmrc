# This is a PHP project; the Node/webpack toolchain (webpack-mix -> node-sass)
# is only used to build front-end assets locally. The compiled output is already
# committed under dist/, so the deploy server does not need dev dependencies.
# Skipping them avoids compiling node-sass (fails on Node 22 / no Python on server).
omit=dev
